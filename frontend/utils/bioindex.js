// Moved to utils/sifter/associationsApi.js, which is the single seam for the
// bioindex index layout. Re-exported so useBioindex() keeps working unchanged.
export { buildAssociationsUrl, fetchAllPages } from "./sifter/associationsApi.js";
